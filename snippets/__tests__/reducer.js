jest.unmock('../reducer');
import reducer from '../reducer';

describe('Some Actions', () => {
  beforeEach(() => {
    // mock and setup stuff here
  });

  it('does something', () => {
    // unit test
  });
});
