jest.unmock('../reducer');
import reducer from '../reducer';

describe('<<name>> reducer', () => {
  beforeEach(() => {
    // mock and setup stuff here
  });

  it('does something', () => {
    // unit test
  });
});
